{
  "version": 2,
  "buildCommand": "(npx prisma migrate resolve --rolled-back add_po_number_column || true) && npx prisma migrate deploy && prisma generate && npm run build && npx tsc -p tsconfig.server.json",
  "outputDirectory": "dist",
  "functions": {
    "api/index.js": {
      "maxDuration": 30,
      "memory": 1024,
      "includeFiles": "{dist-server/**,.env.production,node_modules/.prisma/client/**}"
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
