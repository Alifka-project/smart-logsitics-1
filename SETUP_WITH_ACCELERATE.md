# Setup with Prisma Accelerate

## Steps:

1. Update DATABASE_URL in Vercel to use Prisma Accelerate URL
2. Create tables using DIRECT connection (not Accelerate)
3. Use Accelerate URL for runtime (faster)

